import { Title } from '@mantine/core';

import { StoreLayout } from './StoreLayout';

export const Store = () => {
  return (
    <StoreLayout>
      <Title order={1}>Store goes here</Title>
    </StoreLayout>
  );
};
